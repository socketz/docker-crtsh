# ocsp_monitor configuration.
# The DB connection is passed via the -conninfo flag (see compose.yml).
connopen = 2
connidle = 2
connlife = "10m"

# Default workload
interval = "30s"
batch = 500
concurrent = 50

# Other settings
httptimeout = "30s"